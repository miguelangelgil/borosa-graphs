name: Update IMF Data

on:
  schedule:
    # Cada domingo a las 00:00 UTC
    - cron: '0 0 * * 0'
  workflow_dispatch: # Permite ejecución manual

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Create data directory
        run: mkdir -p data

      - name: Fetch IMF data
        run: python scripts/fetch_imf_data.py

      - name: Check for changes
        id: git-check
        run: |
          git diff --quiet data/imf_debt_data.json || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/imf_debt_data.json
          git commit -m "📊 Update IMF debt data - $(date +'%Y-%m-%d')"
          git push

  deploy:
    needs: update-data
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main  # Asegura que tenemos los últimos cambios

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
